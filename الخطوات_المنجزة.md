# 📋 ملف التوجيه — مخزن نقشات الضجيج للسجاد

> **الإصدار الحالي: v1.1**
>
> ⚠️ **قواعد إلزامية لأي جلسة قادمة (بدون استثناء):**
> 1. بعد كل دفعة إنجاز جديدة — مهما كان حجمها — **يجب** رفع رقم الإصدار
>    (v0.2 → v0.3 → v0.4 ...) في أعلى هذا الملف، وتحديث جدول "سجل الإصدارات"
>    وقسمي "ما تم إنجازه" / "ما تبقى" ليعكسا الوضع الفعلي الجديد.
> 2. **عند انتهاء أي دفعة عمل (أو عند اقتراب انتهاء التوكن المتاح بالجلسة)،
>    يجب تسليم المستخدم نسخة محدَّثة من هذا الملف تلقائياً دون انتظار طلبه —**
>    إما بمفرده أو ضمن ملف الـzip، حسب ما يطلبه المستخدم في تلك اللحظة.
> 3. اسم ملف الـzip الناتج يجب أن يطابق رقم الإصدار دائماً (مثال: `نقشاتv0.3.zip`).

هذا الملف **مرجع تسليم بين الجلسات**: أرسله لي (Claude) في بداية أي محادثة
قادمة حول هذا المشروع، وسأكمل من حيث توقفت دون إعادة قراءة كل الملفات
المرفقة الأصلية من جديد لتوفير التوكن — فقط اذكر لي: "أكمل بناء موقع مخزن
نقشات الضجيج، هذا ملف التوجيه" وأرفق هذا الملف + مجلد `src/` المُنجز.

---

## 🗂️ مصدر التعليمات المتبع

اتُبعت تعليمات `kimi2.6` الموجودة في:
- `carpet-store-build-kit/BUILD_INSTRUCTIONS.md` (الخطوات الثماني الرئيسية)
- `carpet-store-build-kit/04-Ponytail/skills/SKILL.md` (فلسفة البساطة + تعليقات `ponytail:`)
- `carpet-store-ai-toolkit/*` (بيانات العمل، الكلمات المفتاحية، قوالب Schema، محتوى الصفحات)

القالب الأساسي المستخدم: `01-Local-Business-Builder/templates/` (قالب Astro
عام لأعمال محلية، كان بالإنجليزية "Texas Plumbing" — تم تعريبه بالكامل).

---

## ✅ ما تم إنجازه (v0.2)

### الإعداد (Config) — v0.1، بلا تغيير
- [x] `astro.config.mjs`, `package.json`, `tsconfig.json`, `public/robots.txt`

### البيانات (src/data/) — v0.1، بلا تغيير
- [x] `business.ts`, `serviceTypes.ts`, `serviceAreas.ts` (بحقل `nearby`), `seoContent.ts`
- [x] `src/lib/urls.ts` — بنية `/services/{خدمة}/{منطقة}/` مصحَّحة (كما في v0.1)

### الأنماط والمكوّنات والتخطيط — v0.1، بلا تغيير
- [x] `global.css`, `SEO.astro`, `Header.astro`, `Footer.astro`, `Breadcrumbs.astro`,
      `SeoFaq.astro`, `ContactCTA.astro`, `BaseLayout.astro`

### 🆕 صفحات المناطق الست (`src/pages/areas/{slug}/index.astro`) — جديد في v0.2
- [x] السالمية، حولي، الفروانية، الأحمدي، الجهراء، العاصمة
- كل صفحة: H1 + قائمة روابط للخدمات الثلاث في المنطقة (`comboUrl`) + قسم
  "لماذا يختارنا سكان {المنطقة}" (4 نقاط) + قسم "مناطق قريبة نخدمها" (يستخدم
  حقل `nearby` من `serviceAreas.ts` مع روابط داخلية) + Schema نوع `Service`
  مع `areaServed` + Breadcrumbs (الرئيسية > المناطق > اسم المنطقة) + قسم
  أسئلة شائعة (SeoFaq) + ContactCTA بعنوان مخصص للمنطقة.
- **توليد المحتوى:** عبر سكربت بايثون (`gen_areas.py`، غير مضمّن في الـzip) —
  6 صياغات مقدمة مختلفة و6 مجموعات "لماذا يختارنا" و6 صياغات ختامية، مُوزَّعة
  بالتناوب على المناطق الست لضمان عدم تكرار النص حرفياً (تم التحقق: 6 نصوص
  مقدمة فريدة لـ6 صفحات).

### 🆕 صفحات التوليف الـ18 (`src/pages/services/{خدمة}/{منطقة}/index.astro`) — جديد في v0.2
- [x] 3 خدمات (carpets, parquet, artificial-grass) × 6 مناطق = 18 صفحة
- بنية الروابط: `/services/{خدمة}/{منطقة}/` (مطابقة لخطة السيو، **وليس**
  `/services/{منطقة}/{خدمة}/`)
- كل صفحة: H1 مخصص + قائمة الأنواع المتوفرة (مأخوذة من نفس تصنيفات صفحة
  الخدمة الرئيسية) + قسم "لماذا يختارنا سكان {المنطقة}" + قسم "مناطق قريبة
  نخدمها" مع روابط لصفحات توليف نفس الخدمة في المناطق المجاورة (`nearby`) +
  Schema نوع `Service` + `BreadcrumbList` كامل (4 مستويات، مطابق لقالب
  `combination_page` في `05_SCHEMA_TEMPLATES.json`) + Breadcrumbs مرئية +
  SeoFaq مفلترة حسب كلمة مفتاحية الخدمة + ContactCTA.
- **توليد المحتوى:** عبر سكربت بايثون (`gen_combos.py`، غير مضمّن في الـzip) —
  نفس منطق التنويع (6 صياغات لكل جزء) مع إزاحة إضافية حسب رقم الخدمة بحيث لا
  تتكرر نفس الصياغة حتى بين خدمتين مختلفتين لنفس المنطقة. **تم التحقق آلياً:**
  جميع نصوص المقدمة الـ18 فريدة 100% (لا تكرار حرفي).
- **ملاحظة تقنية:** الصفحات كملفات Astro ثابتة فعلية (static file-based)
  مطابقة لنمط v0.1 — لم تُستخدم Astro Dynamic Routes (`[slug].astro` +
  `getStaticPaths`) حفاظاً على اتساق البنية مع الصفحات الموجودة مسبقاً.

### إجمالي الصفحات الآن: 31 من 31 (+ فهرس مناطق إضافي غير محتسب)
`index + about + contact + services/index + 3×صفحة خدمة + areas/index* +
6×صفحة منطقة + 18×صفحة توليف = 31 صفحة رسمية`

كل صفحة (قديمة وجديدة) تحتوي: Title مخصص + Meta description + JSON-LD Schema
مناسب + Breadcrumbs.

## ✅ إضافات v0.3

### 1. تحقق آلي شامل من الكود (بديل عن `astro build` في بيئة بلا إنترنت)
- [x] فحص توازن الأقواس `{ } ( ) [ ]` في جميع ملفات `src/` (44 ملف .astro/.ts)
      — **لا يوجد أي خلل**.
- [x] استخراج كل صفحة فعلية موجودة (32 مساراً) ومطابقتها مع كل رابط `href`
      حرفي (غير متولّد ديناميكياً عبر `comboUrl`/`areaUrl`) في الكود —
      **كل الروابط الداخلية سليمة**؛ الروابط بين صفحات المناطق/التوليف كلها
      تُبنى عبر دوال `src/lib/urls.ts` وليست نصوصاً يدوية، فلا خطر كسر روابط.
  - الاستثناء الوحيد الذي ظهر: مرجعان لـ `/favicon.ico` و`/favicon.svg` في
    `BaseLayout.astro` بلا ملفات فعلية — **تم إصلاحه (انظر البند 2 أدناه)**.
- [x] مراجعة عينة من عناوين H1 لصفحات المناطق الست — تأكيد عدم وجود ركاكة أو
      تكرار حرفي (متوافق مع ما ذكره v0.2).

### 2. 🆕 Favicon حقيقي (كان placeholder فقط)
- [x] `public/favicon.svg` — مونوغرام حرف "ن" بلون العلامة التجارية
      (`#b04a35` من `global.css`) بخط عريض على خلفية مربّعة الزوايا مدوّرة.
- [x] `public/favicon.ico` (متعدد الأحجام 16/32/48/64) بنفس التصميم، مُولَّد
      محلياً بـ Pillow.

## 🚫 قيد دائم على النطاق — Google Business Profile (GBP)

> ⚠️ **إنشاء أو تحسين أو ربط Google Business Profile فعلياً لا يمكن القيام
> به ضمن هذا المشروع** — لا يوجد وصول لحساب Google Business حقيقي لصاحب
> العمل؛ هذه خطوة يقوم بها المستخدم بنفسه خارج نطاق العمل مع Claude.
> تم توثيق هذا القيد في **كل** ملفات التعليمات المرجعية التي تذكر GBP:
> - `carpet-store-build-kit/BUILD_INSTRUCTIONS.md`
> - `carpet-store-build-kit/02-Claude-SEO/skills/seo-local.md`
> - `carpet-store-ai-toolkit/04_BUILD_RULES.md`
> - `carpet-store-ai-toolkit/08_COMPETITOR_ANALYSIS.md`
> - `carpet-store-ai-toolkit/01_SEO_GUIDE.md`
>
> **الأثر على هذا الملف:** أي بند "Google Business Profile" أو "GBP" يظهر
> في الأقسام أدناه (السابقة أو اللاحقة) يُقرأ دائماً على أنه **توصية
> توثيقية للمستخدم فقط**، وليس مهمة معلَّقة سيقوم بها Claude لاحقاً.
>
> **تأكيد صريح من المستخدم (2026-08-17):** هذا القيد نهائي — **لا يُطلب من
> المستخدم لاحقاً بأي صيغة** (لا كسؤال، لا كتذكير، لا كبند "متبقي"). أي
> جلسة قادمة تتجاهل GBP كلياً كأنه غير موجود ضمن نطاق العمل.

## ⏸️ قيد مؤجَّل بطلب المستخدم — روابط السوشيال ميديا

> **تأكيد صريح من المستخدم (2026-08-17):** روابط إنستغرام/فيسبوك/تيك توك
> في `business.ts` تبقى placeholders **عمداً** حتى يطلب المستخدم بنفسه
> إضافتها. **لا تُطلب منه ولا يُذكَّر بها في أي جلسة قادمة** — هذا البند
> مختلف عن GBP (هنا المستخدم *يستطيع* تزويدنا بها لاحقاً إن أراد، لكن
> المبادرة يجب أن تكون منه هو، لا من Claude).

---

## ✅ إضافات v0.5 — صور حقيقية من المستخدم

استُبدلت جميع مسارات placeholder بصور حقيقية أرسلها المستخدم (13 صورة معتمدة
من أصل 16، استُبعدت 3: واحدة تحمل رقم هاتف/تسويق لجهة أخرى، واثنتان تُركتا
جانباً لعدم الحاجة إليهما).

### 1. معالجة الصور
- [x] تحويل جميع الصور المعتمدة إلى `.webp` (جودة 82، أقصى بُعد 1600px) عبر
      Pillow محلياً، وحفظها في `public/images/`.
- [x] إنشاء `public/og-default.webp` (1200×630) — قصّ ذكي + تدرّج سفلي داكن +
      شارة مونوغرام "ن" بلون العلامة التجارية، من صورة سجاد مُركَّب فعلي.

### 2. الربط في الكود (لم يكن هناك أي `<img>` قبل v0.5 — الموقع كان نصياً بالكامل)
- [x] **الرئيسية**: صورة خلفية للـ Hero (شفافة خلف التدرج) + صور مصغّرة حقيقية
      على بطاقات الخدمات الثلاث (بدل الأيقونات فقط).
- [x] **صفحة سجاد**: صورة Hero + معرض "من أعمالنا" (عينات + سجاد مسجد
      مُركَّب فعلياً + عينات سجاد مساجد).
- [x] **صفحة باركيه**: صورة Hero + معرض صورتين للفافات الباركيه.
- [x] **صفحة ثيل صناعي**: صورة Hero + معرض صورتين (حديقة مُنسَّقة + عينة منتج).
- [x] **صفحة "من نحن"**: استُبدلت العبارة الوهمية `[أضف صورة/محتوى حقيقي هنا]`
      بمعرض 3 صور حقيقية.
- [x] `serviceTypes.ts`: حقول `.image` كانت معرَّفة مسبقاً (`hero-carpet.webp`
      وغيرها) لكن غير مربوطة بأي مكان — الآن مربوطة فعلياً وتُطابق أسماء
      الملفات الحقيقية في `public/images/`.

### 3. تحقق آلي بعد التعديل
- [x] توازن الأقواس: 44 ملف، سليم.
- [x] كل مسار صورة مُستخدَم في الكود (`src="/images/..."`) تم التحقق من وجود
      ملفه الفعلي في `public/images/` — لا صور مفقودة.

**ملاحظة:** الصور الحالية مشتقة من صور مرسلة عبر الجوال (بعضها عينات منتج
وبعضها مواقع تركيب أثناء العمل/بعد الانتهاء)، وليست صوراً استوديو احترافية —
جودة مقبولة للاستخدام الفوري، ويمكن استبدالها لاحقاً بصور أعلى احترافية إن
توفرت.

---

## ✅ إضافات v0.6 — خريطة تفاعلية + نموذج تواصل فعلي

كلا التحسينين لا يحتاجان اتصال إنترنت في بيئة البناء ولا بيانات إضافية من
المستخدم (يعملان فعلياً فور رفع الموقع على أي استضافة static)، لذا نُفِّذا
مباشرة من قائمة "ما تبقى" في v0.5.

### 1. 🆕 خريطة جوجل مُضمَّنة فعلياً (`src/pages/contact.astro`)
- [x] استُبدل الرابط النصي بـ `<iframe>` حقيقي (نمط `maps.google.com/maps?q=...&output=embed`،
      بدون الحاجة لمفتاح API) يعرض موقع المتجر مباشرة داخل الصفحة، بالاعتماد
      على `business.coordinates` الموجودة مسبقاً في `business.ts`.
- [x] أُبقي رابط "فتح الاتجاهات على خرائط جوجل" أسفل الـ iframe كخيار إضافي
      للزائر الذي يريد التنقل الفعلي.
- **سقف معروف:** الدبوس تقريبي بإحداثيات `business.ts` الحالية وليس عنوان
  Google Business Profile موثّقاً رسمياً (GBP نفسه خارج نطاق العمل — انظر
  القيد الدائم أعلاه). عند إنشاء GBP فعلي، يُستبدل بر ابط embed الرسمي منه.

### 2. 🆕 نموذج التواصل أصبح فعلياً عبر واتساب (`src/pages/contact.astro`)
- [x] عند الضغط على "إرسال"، سكربت JS بسيط (بدون مكتبات، `<script define:vars>`)
      يجمع الحقول (الاسم، الهاتف، البريد الاختياري، الرسالة) ويبني رابط
      `https://wa.me/{whatsapp}?text=...` مع الرسالة مُنسَّقة، ويفتحه في تبويب
      جديد — بدون أي باك-إند أو خدمة خارجية (Formspree/Netlify Forms)، يعمل
      فوراً على استضافة static بحتة.
- [x] حقل البريد الإلكتروني حُوِّل إلى "اختياري" (لم يعد `required`) لأن قناة
      الإرسال الفعلية الآن واتساب وليس بريداً.
- **سقف معروف:** يفترض أن الزائر لديه واتساب مثبتاً على جهازه (افتراض شائع
  في الكويت)؛ لا يوجد أرشفة/تخزين للرسائل في أي مكان — كل رسالة تذهب مباشرة
  لواتساب صاحب المتجر فقط. لا بريد إلكتروني احتياطي حالياً.

### 3. تحقق آلي بعد التعديل
- [x] توازن الأقواس: 44 ملف، سليم.
- [x] لا تغييرات في أي ملف آخر غير `contact.astro`.

---

## ✅ إضافات v0.7 — بيانات حقيقية من المستخدم (`src/data/business.ts`)

- [x] `owner`: `'[اسم صاحب المتجر]'` → `'حامد الشويطي'`
- [x] `address.zip`: `'[الرمز البريدي]'` → `'80001'`
- **ملاحظة:** الحقلان غير مربوطين حالياً بأي عرض مرئي أو Schema في الكود
  (لم يظهر أي طلب لذلك في ملفات التعليمات المرجعية) — تم تحديث البيانات
  الخام فقط تمهيداً لاستخدامها لاحقاً إن احتاج الأمر.
- [x] تحقق آلي: توازن الأقواس 44 ملف سليم، لا تغييرات في أي ملف غير `business.ts`.

---

## ✅ إضافات v0.9 — أول build فعلي + النشر على GitHub Pages 🚀

**هذه أول جلسة تعمل في بيئة متصلة بالإنترنت** — لذا أُنجز البند الذي ظل معلَّقاً
منذ v0.1 عبر سبع إصدارات متتالية.

### 1. ✅ `npm install` و`astro build` — نجحا فعلياً (البند المعلّق منذ v0.1)
- [x] `npm install`: **367 حزمة** ثُبِّتت بنجاح، بلا أي خطأ. (تحذيرات deprecated
      بسيطة فقط: `whatwg-encoding`, `tsconfck` — غير مؤثرة.)
- [x] `npm run build` (`astro build`): **نجح — 32 صفحة HTML، صفر أخطاء، صفر
      تحذيرات بناء**، في 3.75 ثانية. حجم `dist/` = 3.6MB.
- [x] `sitemap-index.xml` و`sitemap-0.xml` تولّدا تلقائياً عبر `@astrojs/sitemap`.
- **النتيجة:** كل التحقق اليدوي/الآلي في v0.3–v0.7 كان صحيحاً — لا خطأ واحد ظهر
  عند البناء الحقيقي. بنية الملفات الثابتة (بدل Dynamic Routes) عملت كما هو مخطط.
- [x] أُضيف `package-lock.json` (مُولَّد من التثبيت الفعلي) لضمان بناء قابل للتكرار.

### 2. 🆕 النشر على GitHub Pages
- [x] المستودع: `naqshat-aldajeej/naqshat-aldajeej.github.io` (كان فارغاً).
- [x] الفرع: `main` — 69 ملفاً في commit واحد.
- [x] 🆕 `.github/workflows/deploy.yml` — سير عمل GitHub Actions يبني الموقع
      (`npm ci` + `npm run build`) وينشره على Pages تلقائياً **عند كل push
      إلى `main`**. لا حاجة لرفع `dist/` يدوياً أبداً.
- [x] 🆕 `.gitignore` (`node_modules/`, `dist/`, `.astro/`).
- [x] مصدر Pages ضُبط على `workflow` (Actions) عبر الـAPI بدل Jekyll الافتراضي.
- [x] **حالة النشر: success** ✅ — الموقع حي على:
      **https://naqshat-aldajeej.github.io/**
- **ملاحظة:** `astro.config.mjs` كان `site` فيه مضبوطاً مسبقاً على نفس النطاق،
  و`robots.txt` يشير للـsitemap الصحيح — لم يحتج أي تعديل.

### 3. سير العمل من الآن فصاعداً
أي تعديل مستقبلي على الكود يُنشر بمجرد:
```
git add -A && git commit -m "وصف التعديل" && git push
```
ثم ينتظر ~دقيقة ليكتمل سير العمل تلقائياً.

### 4. 🔐 تنبيه أمني
تم استخدام توكن GitHub الذي زوّدنا به المستخدم في هذه الجلسة. **التوكن أصبح
مكشوفاً في سجل المحادثة — يجب حذفه من GitHub وإنشاء غيره.** لا يؤثر حذفه على
الموقع المنشور ولا على سير العمل التلقائي (Actions يستخدم توكن داخلي خاص به).

---

---

## ✅ إضافات v1.0 — أوقات الدوام الجديدة + ملف `llms.txt`

### 1. 🕙 تحديث أوقات الدوام (بيانات حقيقية من المستخدم)
الأوقات القديمة (9ص–9م سبت–خميس، 4م–9م الجمعة) استُبدلت بـ **10:00 ص – 11:00 م
يومياً، سبعة أيام في الأسبوع بما فيها الجمعة**.

- [x] `business.ts` → `hours` (النص المعروض): سطر واحد `السبت - الجمعة: 10:00 ص - 11:00 م`
      بدل سطرين.
- [x] `business.ts` → `hoursSchema` (Schema.org): كتلة واحدة تضم الأيام السبعة
      `opens: '10:00'` / `closes: '23:00'` بدل كتلتين.
- [x] `seoContent.ts` → إجابة سؤال «ما هي ساعات عمل...» في الأسئلة الشائعة
      كانت تذكر الأوقات القديمة **نصاً حرفياً** — صُحِّحت (لولا ذلك لتناقض
      الـFAQ مع التذييل في كل صفحة).
- [x] تحقق آلي بعد البناء: التذييل + صفحة التواصل + JSON-LD كلها تعرض الأوقات
      الجديدة، و**صفر** ملفات HTML تحتوي أي أثر للأوقات القديمة
      (`9:00 ص`, `9:00 م`, `4:00 م`, `"09:00"`, `"21:00"`, `"16:00"`).
- **ملاحظة:** الجمعة صارت مثل بقية الأيام تماماً (لم تعد استثناءً). إن كان
  الدوام يوم الجمعة مختلفاً فعلياً، أبلغني لأفصله من جديد.

### 2. 🆕 `llms.txt` — مرجع منظَّم للنماذج اللغوية
ملف بمعيار [llms.txt](https://llmstxt.org) على `/llms.txt` يمنح النماذج اللغوية
(ChatGPT، Claude، Perplexity، وأدوات البحث الذكية) صورة كاملة عن المتجر في طلب واحد.

- [x] 🆕 `src/pages/llms.txt.ts` — **endpoint يُولَّد آلياً** وليس ملفاً ثابتاً في
      `public/`. يستورد `business.ts` + `serviceTypes.ts` + `serviceAreas.ts` +
      `seoContent.ts` + `lib/urls.ts`، فأي تعديل مستقبلي في البيانات ينعكس على
      `/llms.txt` تلقائياً في البناء التالي — **لا صيانة يدوية ولا خطر تقادم**.
      (هذا بالضبط ما حصل مع أوقات الدوام: التعديل في مكان واحد ظهر في الملف فوراً.)
- **المحتوى (12.6KB، 164 سطراً):** ملخص blockquote + الهوية والبيانات الأساسية
      (المالك، التأسيس، الإحداثيات، العنوان) + التواصل والأوقات + الخدمات الثلاث
      مع **كتالوج الأنواع الكامل** (7 أنواع سجاد، 3 أنواع باركيه، مواصفات الثيل)
      والكلمات المفتاحية + المناطق الست مع المحافظة والمناطق المجاورة + **فهرس
      كامل لكل الصفحات الـ32 بروابط مطلقة** + الأسئلة الشائعة العشرة كاملة +
      قسم «ملاحظات للنماذج اللغوية».
- **قسم الحماية من الهلوسة** (أهم جزء عملياً): يوجّه النماذج صراحةً إلى
      **عدم اختراع أسعار** (الموقع لا ينشر أسعاراً — التسعير عبر الواتساب حسب
      المساحة والنوع) و**عدم اختراع تقييمات أو نجوم** (لا تقييمات معتمدة منشورة)،
      ويشرح أن **«الزل» و«الموكيت»** مصطلحان كويتيان محليان مرادفان للسجاد لا
      منتجان منفصلان — وهو خطأ شائع تقع فيه النماذج مع السوق الكويتي.
- [x] دالة `arabicYears()` لتمييز العدد العربي (7 **سنوات** لا «7 سنة») — الخطأ
      ظهر في أول بناء وصُحِّح.
- [x] `public/robots.txt`: أُضيفت إشارة تعليقية إلى `/llms.txt` (مع الإبقاء على
      سطر الـSitemap كما هو).

### 3. تحقق ونشر
- [x] `astro build`: **32 صفحة + `/llms.txt`**، صفر أخطاء وصفر تحذيرات.
- [x] push إلى `main` → GitHub Actions → **النشر نجح** ✅
- 🔗 https://naqshat-aldajeej.github.io/llms.txt

---

---

## ✅ إضافات v1.1 — تفعيل Google Analytics 4

المعرّف الحقيقي وصل من المستخدم: **`G-7CG6Y612EH`** — أُغلق بذلك آخر بند كان
معلّقاً بانتظار بيانات من صاحب المتجر.

- [x] `business.ts`: حقل جديد `analytics: { ga4: 'G-7CG6Y612EH' }` — المعرّف
      **لم يُثبَّت داخل القالب** بل وُضع مع بقية البيانات، اتساقاً مع نمط المشروع.
- [x] `BaseLayout.astro`: استيراد `business` + وسم `gtag.js` **شرطي** في `<head>`
      (`{business.analytics.ga4 && (...)}`) — السكربت الخارجي `async` وسكربت
      التهيئة `is:inline` مع `define:vars` لتمرير المعرّف.
- **فائدة الشرط:** تفريغ الحقل (`ga4: ''`) يعطّل التتبع كلياً فوراً دون حذف أي
      كود — مفيد للاختبار أو إن أراد المستخدم إيقاف التتبع لاحقاً.
- [x] تحقق آلي بعد البناء: الوسم موجود في **32/32 صفحة**، صفر صفحات ناقصة.
- [x] build نظيف (صفر أخطاء/تحذيرات) + push + **نشر ناجح** ✅

### ما يحتاجه المستخدم بنفسه الآن (خارج نطاق الكود)
1. فتح GA4 → **Realtime** وزيارة الموقع للتأكد من وصول أول جلسة (قد يستغرق دقائق).
2. ربط GA4 بـ **Google Search Console** لاحقاً لرؤية الكلمات المفتاحية مع السلوك.
3. **ملاحظة قياس مهمة:** أهم تحويل في هذا الموقع هو الضغط على واتساب/الاتصال،
   وهذه أحداث خروج لا يلتقطها GA4 كتحويل تلقائياً. **الترقية المقترحة (لم تُنفَّذ):**
   إضافة `gtag('event', 'contact_click', {...})` على أزرار واتساب والهاتف ثم
   تعليمها كـ Key Event في GA4 — بدونها ستُقاس الزيارات فقط لا النتائج.

---

## ⏳ ما تبقى (الأولوية التالية — v1.2)

### 1. تحسينات تقنية
- [x] ~~تشغيل `npm install` و`astro build` فعلياً~~ — **تم في v0.9** (32 صفحة،
      صفر أخطاء)، وأصبح يعمل تلقائياً في كل push عبر GitHub Actions.
- [ ] خطوط Cairo/Tajawal لا تزال تُحمَّل من Google Fonts CDN وليس ملفات محلية.
      **الترقية المقترحة (لم تُنفَّذ — خارج نطاق طلب v0.9):** `npm i @fontsource/cairo`
      واستيراده في `global.css` — هذا يستضيف الخط محلياً ضمن الـbuild ويلغي
      طلب الشبكة الخارجي، ويحسّن LCP ويزيل نقطة تتبّع خارجية.

### 2. مهام تتطلب بيانات فعلية من صاحب المتجر (لا يمكن لـ Claude اختراعها)
- [x] ~~GA4 غير مفعّل~~ — **تم في v1.1** (`G-7CG6Y612EH`، وسم في كل الصفحات الـ32).
      المتبقي اختياري: تتبع أحداث الضغط على واتساب/الاتصال كتحويلات (انظر v1.1).
- [x] ~~اسم صاحب المتجر~~ — تم في v0.7 (حامد الشويطي).
- [x] ~~الرمز البريدي~~ — تم في v0.7 (80001).
- [⏸] روابط السوشيال ميديا (إنستغرام/فيسبوك/تيك توك) — **مؤجَّلة عمداً بطلب
      المستخدم**، لا تُطلب منه لاحقاً (انظر القسم المخصص أعلاه).

### 3. خارج النطاق حالياً
- المدونة (`blog/`) لم تُبدأ إطلاقاً — ليست ضمن نطاق الـ31 صفحة، تجاهلها إلا إذا طُلبت.
- Google Business Profile — قيد دائم، **نهائي، لا يُطلب من المستخدم لاحقاً بأي صيغة** (انظر القسم المخصص أعلاه).

### 4. تم التحقق منها (محدَّث في v0.9)
- ✅ ~~لم يُشغَّل `npm install` ولا `astro build`~~ — **تم في v0.9 بنجاح تام**:
  32 صفحة، صفر أخطاء. المراجعة اليدوية والفحص الآلي في v0.3–v0.7 أثبتا دقتهما.
- ✅ ~~الموقع غير منشور~~ — **منشور في v0.9** على GitHub Pages.
- ✅ ~~لا صور فعلية~~ — تم إنجازه في v0.5 (13 صورة حقيقية من المستخدم).
- ✅ ~~لم يُنشأ favicon فعلي~~ — تم إنجازه في v0.3.

---

## 📊 معلومات العمل الثابتة (للمرجعية السريعة)

```
الاسم: مخزن نقشات الضجيج للسجاد
الهاتف: 65061072  |  واتساب: 96565061072
الموقع: الضجيج، الكويت  |  تأسس: 2019  |  المشاريع: 1700+
الخدمات: سجاد (زل/موكيت) — باركيه — ثيل صناعي
بنية الروابط: /services/{service}/{area}/  (خدمة أولاً ثم منطقة)
المناطق الست: al-salmiya, hawally, al-farwaniyah, al-ahmadi, al-jahra, al-asimah
```

---

## 🔁 سجل الإصدارات

| الإصدار | التاريخ | ملخص |
|---------|---------|------|
| v0.1 | 2026-08-17 | الإعداد + البيانات + المكوّنات + 8 صفحات ثابتة/خدمات (بدون صفحات المناطق الفردية والتوليف) |
| v0.2 | 2026-08-17 | إضافة 6 صفحات مناطق + 18 صفحة توليف (خدمة×منطقة) = 24 صفحة جديدة، مولَّدة عبر سكربت بايثون بمحتوى فريد لكل صفحة لتفادي Duplicate Content. **الموقع الآن مكتمل من ناحية عدد الصفحات (31/31)** لكن لم يُختبر بـ`astro build` بعد ولا صور/favicon حقيقية. |
| v0.3 | 2026-08-17 | فحص آلي شامل: توازن أقواس (44 ملف، سليم) + تحقق من كل الروابط الداخلية (32 صفحة، سليمة) → كشف نقص favicon فعلي فقط. إضافة `favicon.svg` + `favicon.ico` حقيقيين بمونوغرام "ن" بلون العلامة التجارية. `npm install` ما زال يفشل (403 — لا اتصال بالبيئة الحالية)، لذا `astro build` الفعلي لا يزال معلّقاً لبيئة متصلة. |
| v0.4 | 2026-08-17 | توثيق قيد دائم: GBP لا يمكن إنجازه ضمن هذا المشروع (لا وصول لحساب Google Business فعلي). أُضيفت ملاحظة تحذيرية في كل ملفات التعليمات المرجعية الخمسة التي تذكر GBP (build-kit وai-toolkit)، بالإضافة إلى قسم مخصص في هذا الملف. لا تغييرات في كود الموقع نفسه. |
| v0.5 | 2026-08-17 | إضافة 13 صورة حقيقية أرسلها المستخدم (سجاد، سجاد مساجد، باركيه، ثيل صناعي) بعد معالجتها وتحويلها لـ webp، وربطها فعلياً في: الرئيسية (Hero + بطاقات الخدمات)، الصفحات الثلاث للخدمات (Hero + معرض لكل صفحة)، صفحة "من نحن" (استبدال placeholder نصي)، وصورة OG مُصمَّمة (1200×630) للمشاركة الاجتماعية. الموقع كان نصياً بالكامل بدون أي `<img>` قبل هذا الإصدار. |
| v0.6 | 2026-08-17 | صفحة التواصل: (1) استبدال رابط خرائط جوجل النصي بـ `<iframe>` مُضمَّن فعلياً (بدون مفتاح API)، (2) تحويل نموذج التواصل من نموذج بلا معالج إرسال إلى نموذج فعلي يبني رسالة واتساب من الحقول ويفتح `wa.me` مباشرة عبر JS بسيط بدون باك-إند. لا تغييرات في أي ملف آخر. المتبقي (خطوط محلية + GA4) يتطلب إما اتصال إنترنت فعلي أو بيانات حقيقية من صاحب المتجر غير متوفرة حالياً. |
| v0.7 | 2026-08-17 | تحديث `business.ts` ببيانات حقيقية زوّدنا بها المستخدم: اسم صاحب المتجر (حامد الشويطي) والرمز البريدي (80001)، بدلاً من الـ placeholders. لا تغييرات في أي ملف آخر. المتبقي: معرف GA4 وروابط السوشيال ميديا (تحتاج من المستخدم)، والخطوط المحلية/build فعلي (تحتاج اتصال إنترنت). |
| v0.8 | 2026-08-17 | توثيق قيدين صريحين بطلب المستخدم: (1) روابط السوشيال ميديا تبقى placeholders **عمداً** ومؤجَّلة حتى يطلبها هو بنفسه — لا تُطلب منه في أي جلسة قادمة، (2) قيد GBP الدائم أصبح **نهائياً بشكل صريح** — لا يُطلب من المستخدم لاحقاً بأي صيغة. تعليق `ponytail:` مضاف في `business.ts` فوق حقل `social`. لا تغييرات وظيفية في الموقع نفسه. |
| v0.9 | 2026-08-17 | **أول جلسة ببيئة متصلة بالإنترنت.** (1) `npm install` (367 حزمة) و`astro build` نجحا فعلياً لأول مرة منذ v0.1: **32 صفحة، صفر أخطاء، 3.6MB**، مع توليد sitemap تلقائي — أُغلق البند الذي ظل معلّقاً عبر 7 إصدارات. (2) **الموقع نُشر فعلياً** على `https://naqshat-aldajeej.github.io/` عبر GitHub Pages، مع سير عمل GitHub Actions (`.github/workflows/deploy.yml`) يعيد البناء والنشر تلقائياً عند كل push إلى `main`، و`.gitignore` و`package-lock.json`. لا تغييرات في كود الصفحات نفسها. |
| v1.0 | 2026-08-17 | (1) **أوقات الدوام** حُدِّثت لبيانات المستخدم الحقيقية: 10:00 ص – 11:00 م يومياً سبعة أيام بما فيها الجمعة — في النص المعروض و`hoursSchema` (Schema.org) **وفي إجابة الـFAQ** التي كانت تحمل الأوقات القديمة نصاً حرفياً؛ تحقق آلي: صفر أثر للأوقات القديمة في الـ32 صفحة. (2) 🆕 **`llms.txt`** عبر endpoint مُولَّد آلياً من ملفات البيانات (`src/pages/llms.txt.ts`, 12.6KB) — هوية وتواصل وكتالوج أنواع كامل وفهرس الصفحات الـ32 والأسئلة الشائعة، مع قسم يمنع النماذج من اختراع الأسعار والتقييمات ويشرح مصطلحي «الزل/الموكيت». (3) إشارة إليه في `robots.txt`. build نظيف + نشر ناجح. |
| v1.1 | 2026-08-17 | تفعيل **Google Analytics 4** بالمعرّف الحقيقي `G-7CG6Y612EH` الذي زوّدنا به المستخدم: حقل `analytics.ga4` في `business.ts` + وسم `gtag.js` شرطي في `<head>` بـ`BaseLayout.astro` (تفريغ الحقل يعطّل التتبع دون حذف كود). تحقق آلي: الوسم في **32/32 صفحة**. build نظيف + نشر ناجح. أُغلق بذلك آخر بند معلّق بانتظار بيانات من صاحب المتجر. المتبقي اختياري: تعليم ضغطات واتساب/الاتصال كأحداث تحويل في GA4. |
